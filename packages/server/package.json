{
  "name": "@lunhui/server",
  "version": "0.1.0",
  "private": true,
  "description": "轮回渡口 - Fastify API（审问/轮回/记忆）",
  "type": "module",
  "scripts": {
    "dev": "tsx watch --env-file=../../.env src/index.ts",
    "build": "tsc -p tsconfig.json",
    "start": "node --env-file=../../.env dist/index.js",
    "test": "cross-env LLM_MOCK=1 node --env-file=../../.env --import tsx --test src/**/*.test.ts",
    "test:live": "cross-env LLM_MOCK=0 node --env-file=../../.env --import tsx --test src/services/llm-generator.test.ts",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "lint": "eslint src"
  },
  "dependencies": {
    "@lunhui/engine": "*",
    "better-sqlite3": "^13.0.2",
    "fastify": "^5.0.0",
    "fastify-type-provider-zod": "^5.0.0",
    "openai": "^7.3.0",
    "zod": "^4.4.3"
  },
  "devDependencies": {
    "@types/better-sqlite3": "^9.6.0",
    "cross-env": "^10.1.0"
  }
}
